ticker: AAPL
short_threshold: 0.40
long_threshold: 0.52
model_type: logistic
calibration: platt
risk_profile: normal
tradable: true
note: "AAPL 1y@1h logistic/platt (non-adaptive seed 42): strat ≈15.2%, bench ≈18.0%, alpha ≈-2.8 pts, PF≈1.05, Sharpe≈1.1, max DD≈10.3%. Adaptive (experimental, opt-in) with EV/trend filters: strat ≈5.9%, PF≈1.17, Sharpe≈0.47, DD≈13.6%, 29 trades (safe transition once)."
risk:
  risk_pct: 0.015
  atr_multiplier: 1.5
  reward_multiple: 2.0
  min_position_size: 5
risk_profiles:
  safe:
    risk_pct: 0.01
    atr_multiplier: 1.3
    reward_multiple: 1.5
    min_position_size: 3
  normal:
    risk_pct: 0.02
    atr_multiplier: 1.5
    reward_multiple: 1.8
    min_position_size: 5
  aggressive:
    risk_pct: 0.03
    atr_multiplier: 1.8
    reward_multiple: 2.0
    min_position_size: 5
default_risk_profile: normal
adaptive:
  enabled: false
  lookback_trades: 25
  default_regime: normal
  regimes:
    safe:
      min_equity_drawdown: 0.08
      max_equity_drawdown: 1.0
      min_profit_factor: 0.9
      min_sharpe: -0.5
      risk_overrides:
        risk_pct: 0.01
        reward_multiple: 1.5
      threshold_overrides:
        long_shift: 0.02
        short_shift: -0.02
    normal:
      min_equity_drawdown: -0.01
      max_equity_drawdown: 0.08
      min_profit_factor: 1.0
      min_sharpe: 0.0
      risk_overrides: {}
      threshold_overrides: {}
    aggressive:
      min_equity_drawdown: -0.05
      max_equity_drawdown: -0.01
      min_profit_factor: 1.2
      min_sharpe: 0.5
      risk_overrides:
        risk_pct: 0.025
        reward_multiple: 2.0
      threshold_overrides:
        long_shift: -0.02
        short_shift: 0.02
min_ev_multiple: 0.2
enable_trend_filter: true
trend_long_min: 0.001
trend_short_min: 0.001
