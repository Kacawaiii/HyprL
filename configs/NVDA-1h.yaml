ticker: NVDA
threshold: 0.40
short_threshold: 0.40
long_threshold: 0.55
model_type: logistic
calibration: platt
risk_profile: normal
tradable: false
note: "NVDA 1y@1h logistic/platt (non-adaptive seed 42): strat ≈-1.5%, bench ≈28.0%, alpha ≈-29.5 pts, PF≈0.98, Sharpe≈-0.01, max DD≈14.7%, 78 trades; high-threshold 0.65 gives ≈0.37% (2 trades) so keep calibration-only."
risk:
  risk_pct: 0.015
  atr_multiplier: 1.3
  reward_multiple: 1.5
  min_position_size: 5
risk_profiles:
  safe:
    risk_pct: 0.008
    atr_multiplier: 1.1
    reward_multiple: 1.3
    min_position_size: 3
  normal:
    risk_pct: 0.015
    atr_multiplier: 1.3
    reward_multiple: 1.5
    min_position_size: 5
default_risk_profile: normal
adaptive:
  enabled: false
  lookback_trades: 20
  default_regime: normal
  regimes:
    safe:
      min_equity_drawdown: 0.12
      max_equity_drawdown: 1.0
      risk_overrides:
        risk_pct: 0.008
      threshold_overrides:
        long_shift: 0.03
        short_shift: -0.02
    normal:
      min_equity_drawdown: -0.01
      max_equity_drawdown: 0.12
      risk_overrides: {}
      threshold_overrides: {}
